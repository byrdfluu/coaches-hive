import { NextResponse } from 'next/server'
import { requireSuperadminApi } from '@/lib/adminApiAuth'
import { supabaseAdmin } from '@/lib/supabaseAdmin'
export const dynamic='force-dynamic'
export async function GET(request:Request){const auth=await requireSuperadminApi();if(auth.error)return auth.error;const p=new URL(request.url).searchParams,q=(p.get('query')||'').toLowerCase(),status=p.get('status')||'';let db=supabaseAdmin.from('mobile_checkout_handoffs').select('nonce,user_id,checkout_type,resource_id,status,stripe_checkout_session_id,expires_at,fulfilled_at,last_error,created_at,updated_at').order('created_at',{ascending:false}).limit(250);if(status)db=db.eq('status',status);const {data,error}=await db;if(error)return NextResponse.json({error:'Unable to load mobile handoffs.'},{status:500});const items=(data||[]).map((r:any)=>({...r,id:r.nonce})).filter((r:any)=>!q||JSON.stringify(r).toLowerCase().includes(q));return NextResponse.json({items,summary:{handoffs:items.length,open:items.filter((r:any)=>['issued','processing'].includes(r.status)).length,fulfilled:items.filter((r:any)=>r.status==='fulfilled').length,failed:items.filter((r:any)=>r.last_error).length}})}
